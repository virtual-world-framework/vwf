properties:
    xAPI:
        get: |
            return {

                configure: function(config){
                    vwf.callMethod(this.id, 'xapiConfigure', config);
                }.bind(this)

            }
