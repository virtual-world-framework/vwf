description:
  The sandtable application allows a user to draw and place push pins using multiple colors.  The user can also use the mouse to spin the terrain after selecting the circular arrows or by using the WASD keys.