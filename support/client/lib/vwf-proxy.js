define( function() {

    // This is a placeholder module that allows the vwf/model/* modules to pretend that they are
    // loading the vwf kernel as a module. Once vwf is converted, the models can load it directly,
    // and this module will go away.

    return vwf;

} );
