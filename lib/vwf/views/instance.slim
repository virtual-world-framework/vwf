h1 Instance

pre: code == @storage.state.to_yaml

h2 Revisions

- @storage.revisions.each do |id, revision|
    p: a href=( to revision_url revision.id, "html" ) = revision.id

h2 States

- @storage.states.each do |id, state|
    p: a href=( to revision_url state.id, "html" ) = state.id

h2 Actions

- @storage.actions.each do |id, action|
    p: a href=( to revision_url action.id, "html" ) = action.id
